

export default function cards ({title, description}: {title: string, description: string}) {
    return (
        <div className="bg-black w-full flex items-center justify-center text-center">
                <p>{title}</p>
                <p>{description}</p>
        <div className="flex flex-col ">
          

        </div>
        </div>
    )
}